define(function(require, exports, module){
    // var Handlebars = require('handlebars');
    // var itemTpl = require('tpl/CredentialList.tpl');
    // var ImageLoad = require("ImageLoad");
    // var App = function(){
    //     return {
    //         /**
    //          * 初始化入口
    //          * 
    //          */
    //         init:function(){
    //             App.renderData();
    //         },
    //         renderData:function(){
    //             G.get(Service.getAllCredentials,{},function(data){
    //                 if(data.lists){
    //                     $.each(data.lists,function(k,item){
    //                         item.thumb = G.imagesURl + item.thumb;
    //                     }); 
    //                 }            
    //                 var itemHtml = Handlebars.compile(itemTpl);
    //                 $(".certList").html(itemHtml(data));
    //                 // var imageLoad = new ImageLoad();
    //             });
    //         }
    //     };
    // }();
    // G.onReady(App);
});